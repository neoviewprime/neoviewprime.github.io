services:
  chromadb:
    image: chromadb/chroma:latest
    container_name: neoview-chromadb
    restart: unless-stopped
    ports:
      - "8000:8000"
    volumes:
      - neoview_chroma_data:/chroma/chroma

volumes:
  neoview_chroma_data:

